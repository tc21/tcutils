""" The subfiles module provides the ability to iterate through all
    files or folders in a directory and its subdirectories. """

from .subfiles import get_dirs, get_elements, map_dirs, map_elements, Limit
